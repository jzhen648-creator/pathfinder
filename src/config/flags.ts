/**
 * FEATURE FLAGS — Turn Features On and Off
 *
 * Set any flag to true to enable a feature.
 * Set to false to hide it completely.
 *
 * SAFE TO EDIT — changes here never break
 * anything, they only show or hide features.
 */

export { FLAGS, isEnabled, type FeatureFlag } from "@/lib/flags";


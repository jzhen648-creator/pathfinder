/**
 * COMPONENT — Node Preview
 *
 * Preview state before new nodes are saved to the map.
 * This is a placeholder adapter while preview rendering lives in `pathfinder-home.tsx`.
 */

export function NodePreview() {
  return null;
}


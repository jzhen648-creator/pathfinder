/**
 * COMPONENT — Error Boundary
 *
 * Re-export of the shared error boundary from legacy location.
 * Use this path for plain-English discoverability.
 */

export { ErrorBoundary } from "@/components/ErrorBoundary";


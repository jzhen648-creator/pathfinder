/**
 * COMPONENT — Branch Prompt
 *
 * Click target shown for empty branch guidance.
 * This is a placeholder adapter while prompt rendering lives in `pathfinder-home.tsx`.
 */

export function BranchPrompt() {
  return null;
}


/**
 * COMPONENT — Node Circle
 *
 * Renders a single map node visual.
 * This is a placeholder adapter while node rendering lives in `pathfinder-home.tsx`.
 */

export function NodeCircle() {
  return null;
}


/**
 * COMPONENT — Node Info Panel
 *
 * Hover/selection details panel for a map node.
 * This is a placeholder adapter while panel rendering lives in `pathfinder-home.tsx`.
 */

export function NodeInfoPanel() {
  return null;
}


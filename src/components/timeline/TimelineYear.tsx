/**
 * COMPONENT — Timeline Year
 *
 * Renders one year section in the timeline.
 * This is a placeholder adapter while timeline rendering lives in `TimelineView.tsx`.
 */

export function TimelineYear() {
  return null;
}


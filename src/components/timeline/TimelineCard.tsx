/**
 * COMPONENT — Timeline Card
 *
 * Renders one event card in the timeline.
 * This is a placeholder adapter while timeline rendering lives in `TimelineView.tsx`.
 */

export function TimelineCard() {
  return null;
}


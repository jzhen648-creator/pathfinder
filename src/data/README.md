DATA
This is where configuration and shipped assets live.

- `branches.ts` — re-exports life-area config from `src/lib/life-areas.ts`
- `pathfinder-tree-geometry.json` — default tree layout geometry (Save geometry export baseline)

Dev seed fixtures (e.g. Jeremy profile for `npm run seed:tree`) live in `src/lib/fixtures/`.

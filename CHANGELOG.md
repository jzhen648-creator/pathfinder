# Changelog

> **Active client:** `pathfinder-mobile/`. API changes here affect mobile. Desktop tree UI paused — [DESKTOP-ON-HOLD.md](./DESKTOP-ON-HOLD.md).

Hand-maintained work log, **not updated since 2026-05-22** — for anything later, use `git log` and [`DECISIONS.md`](./DECISIONS.md) (which stayed current). The stale "local WIP" note that used to live here referred to the 2026-05-16 section, long since committed.

## 2026-05-22

- **Hub panel UX:** Unified **Open Stream** on theme/hub/pursuit panels; removed hub question chips, duplicate stream links, and manual **Add mark**. Hub panel lists existing marks.
- **Cleanup:** Removed unwired manual mark create flow from `TreeView` (`onAddMoment`, mark modals). Pursuit Stream placeholder no longer implies marks attach to pursuits.
- **QA:** `e2e/hub-panel-stream.spec.ts` (Mandela Friendships hub).

## 2026-05-19

- **Docs:** Refreshed [`BRIEF.md`](./BRIEF.md), [`DECISIONS.md`](./DECISIONS.md), [`GLOSSARY.md`](./GLOSSARY.md), [`ONTOLOGY.md`](./ONTOLOGY.md), [`docs/STABILIZATION.md`](./docs/STABILIZATION.md), [`docs/DIARY.md`](./docs/DIARY.md), [`AGENTS.md`](./AGENTS.md) for the May 2026 tree/Stream sprint (see bullets below).
- **Mark detail UX (G1):** Timeline notes use **`MarkHoverCard`** (hover + pin on the map) instead of a bottom-sheet `moment` panel in `TreePanel`. Theme / hub / pursuit panels stay in the left **rail** when open.
- **Soft delete (G4):** `Goal.archived` / `Mark.archived` hide rows from the tree; hub panel **Archive** section with revive (`PATCH` `archived: false`). Remove copy says “archived,” not hard-deleted.
- **Marks hub-level only (G5):** Hub panel lists existing marks; direct panel **Add mark** buttons are removed. Stream prompts: marks belong to the hub, never `pursuitRef`.
- **Tree hit targets:** Removed clicks on limb hull polygons, wide limb-stem strokes, and hub branch-line labels. Theme navigation uses gateway/icon + label row only; limb focus (when enabled) toggles from the theme icon, not backdrop geometry.
- **Edit map drag-and-drop (G10):** Tree toolbar **Edit map** mode (disabled during Stream). Drag pursuits — hub ring moves to another hub (`POST /api/goals/[goalId]/reorganize` `moveToHub`), pursuit target nests (`reparent`), branch slots reorder via `sequenceAnchor`. Pan disabled while editing; 5px drag threshold preserves tap-to-open panel. After edits, opening Stream pre-fills an acknowledgement draft.
- **Ambiguous on tree (G6):** Stream `ambiguous[]` items commit immediately as unresolved marks (`needsResolution`, dashed ring + `?` on tree). Resolve via mark hover card (Done / In progress / Not started) or `POST /api/stream/resolve-ambiguous`. Hub panel shows “N items need your input”. Confirmation queue skips ambiguous cards; informational banner only.
- **Panel redesign (G2):** Theme panel — one-line about, stats in header, **Open Stream**, scannable hub list only (removed evaluation, stats grid, prompt chips, all-pursuits list). Hub panel — **Open Stream**, compact about copy, marks list, active pursuits + **Show more** for on hold/complete; removed hub question chips and manual **Add mark**. Pursuit panel — **Active / On hold / Complete** buttons (`PATCH` `bloomStatus`) and unified **Open Stream**.
- **Evolve removed:** Deleted `evolve-goal-proposal.ts`, `POST /api/goals/[goalId]/fork`, and `POST /api/goals/[goalId]/fork/propose`. Tree panel no longer offers **Evolve this pursuit**; **Stream** is the supported path for new pursuits/marks/milestones. Continuation geometry renamed to `continuationChildScreenPosition`; `parentGoalId` data unchanged. E2E: `milestone-bloom-evolve.spec.ts` keeps milestone → bloom only.
- **Stream status changes:** Hub + theme extract prompts (Rule 9) map finished → `existingGoalId` + `COMPLETE`, paused → `PAUSED`, resuming → `ACTIVE` — no new pursuit for status-only dumps. Commit path applies `PAUSED` / `ACTIVE` updates on existing goals.
- **Mark placement (G9):** Marks share `sequencedNodes` rank with pursuits but render **beside** the branch ray (lateral offset). Amber **diamond** on canvas (no labels); detail in hover card. `TreeMarkNode` + `branchMarkScreenPosition`.
- **Sparse context (G8):** `POST /api/stream/enrich` enriches short marks/pursuits from user notes via Gemini. “Want to add more context?” on mark hover card and pursuit panel when title &lt; 5 words and no description.
- **Hub taxonomy v6 + enriched catalog:** `TAXONOMY_VERSION` → `2026-05-19-v6`. Renamed **Projects** → **Builds & Launches**, **Mind** → **Inner life**. All 17 hubs get `about`, `why`, `belongsHere`, `doesNotBelongHere`, `aiRoutingNote` in `hub-catalog.ts`. Stream extract passes routing notes (theme extract includes belongs/does-not lists). Hub panel shows full catalog sections with accent-coloured redirect targets. Dev preview: `/dev/hub-panel-preview`.

## 2026-05-16

*(Local WIP — not on `HEAD` as of last sync.)*

- **Branch-level moments + insert-and-reflow (opt-in, flag `BRANCH_LONGITUDINAL_ALL`):** New explicit ordering across `Goal` and `Mark` via `sequencePosition`, plus `Mark.kind` (`mark` / `stream`) to distinguish AI Stream provenance. Migration `20260516040000_add_node_sequence_position_and_mark_kind` + idempotent backfill `npm run backfill:node-sequence` (47 branches, 5 goals + 201 marks on local dev DB at first run). Reusing the existing `Mark` model — no new `Moment` table; canonical product name is **timeline note** (per `GLOSSARY.md`).
- **Refined longitudinal grammar (when flag on):** Every theme drops the fixed-orbit domain-cluster layout. `tree-renderer-grammar.ts` short-circuits `shouldDomainClusterThread` to `false`, and `tree-branch-geometry.ts` adds `branchNodeScreenPosition`, `branchPathForNodeCount`, `branchTipPxForNodeCount`, `branchTipPointForNodeCount`, `branchOutwardUnitFromCatalog`. Goal + moment positions both route through the same rank formula `hub + outwardDir × (BRANCH_HEAD_OFFSET_PX + rank × BRANCH_NODE_SPACING_PX)`. Branch tip scales linearly with rank max; no compression. Continuation children (`Goal.parentGoalId`) keep `evolvedChildScreenPosition` (parent-anchored satellites, unchanged).
- **Insert APIs (live regardless of flag):** Shared anchor resolver `src/lib/branch-sequence.ts` (`append` / `after` / `before` / `between`, fractional midpoint with `MIN_SEQUENCE_GAP = 1e-3` reindex). Wired into `POST /api/goals` and `POST /api/marks` (new optional `anchor` body field). New `POST /api/branches/[branchId]/nodes` for unified kind-tagged insertion (AI Stream entry point). New `PATCH /api/branches/[branchId]/reorder` for explicit drag/drop reorder.
- **Tree data:** `DomainHubData.sequencedNodes` co-sorts goals + marks + legacy `Goal{goalType: moment|event}` rows by `sequencePosition`. Tree-view-fit samples the dynamic branch tip when the flag is on.
- **Out of scope (separately scoped follow-up):** retirement of `Goal.goalType: moment|event` rows and `/api/moments/[id]`. The transitional union in `tree-data.ts` keeps the visual correct in both row flavors meanwhile.
- **Theme & hub taxonomy v5 (17 hubs):** `TAXONOMY_VERSION` → `2026-05-16-v5` in `src/lib/taxonomy.ts` — five themes, **17** default root hubs (removed Network, Reflection, Habits, Community, Upgrades, Recovery). Work theme label **Work & Career**; health theme **Health & Body** with Movement, Nutrition, Appearance, Rest. Legacy hub rows migrate via `LEGACY_HUB_MIGRATIONS` + `syncHubTaxonomyForUser` (`hub-taxonomy-sync.ts`). Finance hubs: Income, Assets, Safety net, Liabilities. See [`DECISIONS.md`](./DECISIONS.md).
- **Progressive hub reveal:** `Branch.isActive` + `Branch.isSystemHub` (migration `20260517100000_add_branch_hub_visibility`) — system taxonomy hubs flagged and wipe-protected; hubs with existing goals/marks backfilled active. **`POST /api/branches/activate`** turns on selected hubs; tree only renders active roots. `src/lib/system-hubs.ts` seeds locked template on profile creation.
- **AI Stream (hub brain-dump):** `POST /api/stream/extract` + `POST /api/stream/commit` (Gemini), `src/lib/ai/stream-extract.ts`, `src/lib/stream-commit.ts`, confirmation card UI under `src/components/stream/`. Extracts pursuits, timeline notes, and ambiguous items; per-item confirm/skip with anchor-aware commit into goals/marks. `Mark.kind = stream` when committed from Stream. E2E: `e2e/stream-confirmation-cards.spec.ts`.
- **Stream session persistence (V2 foundation):** `StreamSession` table keyed by `limbId` (theme) stores full `inputText`, `inputMode`, and commit counts after theme `commitStreamToTheme` (outside the tree transaction). Theme extract loads the last 3 prior dumps into `buildStreamThemeContextInput` for cross-session dedup.
- **Goal map labels:** `Goal.shortLabel` (migration `20260515165300_add_goal_short_label`) + `npm run backfill:short-labels` + `src/lib/goal-short-label.ts` for compact tree canvas text. `Goal.timelineStart` (migration `20260516120000_add_goal_timeline_start`) for swimlane ordering.
- **Onboarding v2:** Replaced chat onboarding with **`onboarding-flow.tsx`** — name + pick **≥2** themes → `POST /api/onboarding/complete` activates matching system hubs. `onboarding-gate.ts` + middleware redirect; `onboarding-resume-redirect.tsx`. Removed `onboarding-chat.tsx`, legacy `src/lib/onboarding.ts`, and dev **`mock-reset` / `mock-users`** routes (use `reset:all-accounts` / `wipe:all-user-data` scripts instead).
- **Tree polish:** Ambient **GROWING** goal halo (`tree-goal-ambient-breathe`); milestone panel **+/−** subtask expansion; roadmap **Detail** toggle for descriptions/subtasks; first/last orbital dots slightly larger by `position`. Connector stroke helper (`tree-connector-strokes.ts`), label LOD (`tree-label-lod.ts`, `tree-svg-text-label.tsx`), `tree-view-fit.ts`, trunk slot table (`tree-trunk-slots.ts`). **`POST /api/marks/parse`** natural-language → mark fields. Removed central `src/data/mock-data.ts` in favour of `src/lib/fixtures/` + profile seeds.
- **Docs:** `GLOSSARY.md`, `ONTOLOGY.md`, `BRIEF.md`, `docs/STABILIZATION.md` aligned to taxonomy v5, Stream, and hub activation. Rationale for sequence grammar and taxonomy: [`DECISIONS.md`](./DECISIONS.md).

## 2026-05-14

- **Milestone convergence (complete):** Dropped `Goal.treeMilestones` JSON column (migration `20260513220000_drop_goal_tree_milestones`). Relational `Milestone` / `Subtask` is the only store; `PATCH /api/goals/[goalId]` is metadata-only. Backfill: `npm run backfill:tree-milestones` for old DBs before migrate.
- **Continuation UX:** Goal achieved banner on last milestone / bloom transition; **Evolve this goal** available anytime (plan ahead); fork / propose APIs allow non-bloomed parents.
- **Tree UX:** Delete goal preserves pan/zoom (`loadData({ silent: true })`, no `clearAll()` on delete).
- **Testing:** `e2e/milestone-bloom-evolve.spec.ts` — critical path (milestone → bloom → fork) + early fork/propose; shared `e2e/helpers/e2e-session.ts` (NextAuth API login).
- **Docs:** `docs/STABILIZATION.md` and `AGENTS.md` aligned to post-convergence state.
- **Dogfood pass (commits `fb4285d`…`70f44e3`, same day):** Login default **`/tree`**; tree **fit-to-hubs** on first load; dashboard CTA **Open life tree**. **Evolve goal** propose → revise → commit: `src/lib/evolve-goal-proposal.ts`, **`POST /api/goals/[goalId]/fork/propose`**, panel wiring; correction loop on `previousProposal`. Hub **conduit** geometry inset before icons; **evolved child** goals spawn along hub→parent ray (distance tuned in `b369e60`, `0616584`). **Flow lines** extend through evolved chains (`0c9395b`) and **highlight** when the child is selected (`70f44e3`). **Health & Body** hub cluster matches four-hub theme shape (`ba56f49`). E2E expanded in `e2e/milestone-bloom-evolve.spec.ts` (`92824de`). `.gitignore` Playwright `test-results` / reports (`1856023`).

## 2026-05-11

- **Tree legibility (brightness):** **Uniform luminance across life-area limbs** — `tree-render-staging.ts` no longer applies plane- or limb-specific filters/opacity/stroke/hull tiering (draw order + per-limb `transform` only; focus dimming unchanged). `tree-render-materials.ts` — `intraLimbBranchDepthMul`, `branchIndexDepthRenderingMul`, and `branchDistalCalmOpacityMul01` are neutral (no default outer/tip darkening). **Global brighten** — shared **`TREE_MAP_SURFACE_FILL`** / **`TREE_MAP_SURFACE_RGB`** in `tree-view-constants.ts` (replacing ad hoc `#07060A`); `tree-svg.tsx` pan root `<g>` **`brightness(1.12)`**; lighter eco/stage overlay fills and lower opacities; **material** floors raised (significance, stroke envelope, conduit length calm, pressure cap). PDF export background uses the same token (`tree-view.tsx`). **`pf-roadmap-theme.ts`** — slightly brighter light and dark **`--rm-canvas`** / **`--rm-canvas2`** (and dark **`--rm-bgEl`**). Narrative: **`docs/DIARY.md`** (same date).
- **Tree map composition (render-only):** `tree-render-staging.ts` + `tree-svg.tsx` — **asymmetric** draw order, per-limb drift/scale around the trunk column (`transformOrigin`), **dominant** read for **Who I'm Becoming** + **People & Relationships**, whole-scene **translate/rotate** bias, stronger **backdrop / south weight / edge vignette** gradients, trunk **emergence** and outer-mass nudges, optional **hull layer** presence multiplier, and **intra-limb** branch depth spread. Follow-up: pulled back **whole-limb opacity/stroke/hull** recession so subordinate limbs stay **alive** (depth via **tonal** filters and ordering, not “disabled quadrants”); raised hull floor, gentler **focus** dimming multipliers, tighter intra-limb tip falloff. No routing, fork, or hull-geometry changes.
- **Tree rendering (incremental):** Milestone-driven visual phase helper (`goal-node-render-phase.ts`) — orbital **dual-layer glow** behind crisp dots, **core glyph** opacity tied to progress; **coherence** via faint quadratic chords between adjacent completed orbitals + adjacency glow scaling + tiny hex **branch-out** accent; **harmonizing/bloomed** goals get a very soft **asymmetric environmental ellipse**, a short **catalog vitality** stroke feeding the goal, and **deterministic organic variance** on chords/glow radii (no new randomness). Wiring in `tree-render-goals-subtree.tsx` / `tree-svg.tsx`.
- **Milestone symbolic completion:** Prisma **`Milestone.completedAt`** (nullable) + migration `20260511143000_add_milestone_completed_at`. Shared semantics **`milestoneDoneForSemantics`** (`src/lib/milestone-semantics.ts`) — explicit timestamp wins; transitional dual-read = all subtasks complete only when **≥1** subtask; **removed vacuous “0 subtasks ⇒ complete”** for lifecycle, projection, and roadmap. **`PATCH /api/goals/[goalId]/milestones/[milestoneId]`** toggles completion and triggers **`recomputeGoalBloomStatus`**. Tree panel: relational milestones use a **single ritual row** per stage (tactile/symbolic, not checkbox-heavy); **`onSetMilestoneCompletion`** from `tree-view.tsx`. Legacy import of completed orbitals into relational rows sets **`completedAt`** (`append-canonical-tree-milestone.ts`). Narrative recap: **`docs/DIARY.md`**.

## 2026-05-10

- **Docs & product framing:** Added [`DECISIONS.md`](./DECISIONS.md) for dated decisions (six-area taxonomy including **Pleasures**, default thread names, Money & Finance thread copy, provisional Pleasures fork placement, **Focus Mode** interaction and `FLAGS.FOCUS_MODE`, and the **Stream** brain-dump → tree-enrichment concept). Refreshed [`BRIEF.md`](./BRIEF.md) so the brief matches a **tree-first** home (`/tree`), milestones/marks on threads, and the Stream narrative; trimmed a few resolved items from [`docs/BACKLOG.md`](./docs/BACKLOG.md).
- **Auth & API surface:** Added [`src/middleware.ts`](./src/middleware.ts) so pages (except `/login`) and `/api/*` (except `/api/auth/*`) require a NextAuth session; unauthenticated API calls return **401**. Added [`.env.example`](./.env.example) for local setup hints.
- **Goals & AI helpers:** Prisma **`Goal.treeMilestones`** (`Json`, default `[]`) plus migration `20260510193000_add_goal_tree_milestones` for an optional **orbital milestones overlay** on the tree (`[{ id, title, completed }]`, separate from roadmap `Milestone` rows). New authenticated routes **`POST /api/goals/parse`** (natural language → structured goal fields via Gemini) and **`POST /api/goals/suggest-milestones`** (milestone title suggestions). **`POST /api/finance/reflection`** replaces the old life-map finance reflection route. Goal APIs and serialization updated to read/write `treeMilestones` where applicable.
- **Tree & shell:** Continued iteration on branch geometry, forks, SVG rendering, goal visuals, panel behaviour, mock/tree geometry JSON, flags, and types; added **limb backdrop hit bounds** and a **conversational goal-create** flow component; dev/mock reset paths gained **tree profile** reset support (`/api/dev/reset-tree-profiles`). Seeding and tree-test scripts now align with the expanded life-area model and test profiles (`tree-test-profiles-seed`, `new-profile-tree-branches`, `api-branch-row` helpers).
- **Legacy removal (in progress on branch):** Removed the standalone **`/life-map`** route and the large legacy **`pathfinder-home`** / guided-overlay / old map component stack, plus unused **`src/screens/*`** wrappers, **`src/ai/*`** stubs, life-map-specific **`/api/life-map/*`** routes (drag suggestion, extract, suggestions, finance reflection), **`src/hooks`** life-map helpers (`useLifeMap`, `useUndo`, `useViewToggle`, `useZoom`), **`guided-flows`** / **`branch-angles`** data, and broad **`map-geometry` / `life-map-v2-layout`** helpers that only served the retired canvas. **Per-goal roadmap page** `src/app/roadmap/[goalId]/page.tsx` removed in favour of shell-driven navigation. **Dashboard** and **login** imports simplified after dropping screen indirection.
- **Roadmap & marks:** Roadmap shell adjustments; **`create-mark-modal`** lives under roadmap components. **Add Goal** modal and chrome/onboarding touches aligned with the slimmer routes.
- **Finance app:** Finance tracker page and `src/database/finance.ts` updated alongside the new finance API location.
- **Developer experience:** **`src/app/dev/icons-preview`** and **`src/components/icons`** for icon inventory; **`scripts/extract-icon-spec-svg.mjs`**, **`scripts/clear-all-goals.ts`**; static assets under **`public/dev/`** and design **`reference/`** material. **ESLint** config and **Playwright** config tweaks; **package.json** script/dependency housekeeping.
- **Workspace:** Parent repo submodule pointer updated to track branch **`feat/goal-creation-and-tree-improvements`** (local commits ahead of origin include tree refactor, rename pass, glossary/docs, and related chores—see git log in `pathfinder/`).

## 2026-05-09

- Refactored the life tree UI: split the monolithic `tree-view` into `tree-svg`, `tree-panel`, and shared modules (geometry, coords, goal rendering, stats HUD, and related helpers) so layout and drawing logic are easier to navigate and change.
- Completed a product-wide rename pass so life areas, goals, and related copy stay aligned with the canonical data model across roadmap, dashboard, onboarding, APIs, and shared types.

## 2026-05-08

- **Data model:** Merged the legacy **Moment** concept into **Goal** (Prisma schema, migration scripts, verifier tooling).
- **Create goal:** Optional `Goal.unit` with migration; extended `POST /api/goals` with create-goal validation; dashboard and next-steps wiring; tree test seed fixtures and a script to remove legacy demo personas; removed the older inline create-goal modal where superseded by the shared shell flow.
- **UX:** Add Goal modal reachable from the tree branch panel, a `goals-changed` event to refresh tree state after saves, and toast feedback.
- **Testing:** Playwright configuration and an e2e spec covering Add Goal creation.
- **Tree rendering:** New straight-limb pipeline with asymmetric thread fan and stem swap; removed unused spine/mirror pipeline code, gated the debug overlay, and renamed the `mirrorAreas` prop for clarity. (A follow-up experiment with golden-ratio thread spacing, fork expansion past four threads, and goal nodes beside threads was merged then **reverted** the same day.)
- **Tooling / quality:** Tighter typing in Prisma seed for legacy life-map upserts (`LegacySeedMoment`, narrowed branch delegates); dev mock-user tree selector aligned with app chrome; ESLint fixes in tree-view.
- **Docs:** Added `GLOSSARY.md` as a terminology reference, finished roadmap-related renames in docs, and recorded **`limbId` legacy** naming where the schema still uses that column for the life area id.

## 2026-04-24

- **Marks migration:** Introduced `Mark` / `Reframe` persistence and marks APIs, removed legacy moments routes, and updated map rendering to fan branch stems with marks on branches plus hover-first label behaviour.
- **Limb map home:** Replaced the dense map with a static **Self + five limbs** card layout (spokes, count badges) and a per-limb detail view for timelines and guided add; removed dead legacy map subtree and unused helpers; restored pragmatic typing on the large home surface where needed; ignore local Prisma SQLite files under `prisma/`; added `docs/BACKLOG.md` for deferred typing and ESLint cleanups.

## 2026-04-23

- Manual branch creation (Slice 1 — data + chooser UI): loaded canonical `Branch` records into client state (from `/api/branches` in real mode, from `MOCK_DATA.branches` in mock mode) and threaded `branchId` through `addQuickNode` / `commitPendingGuidedNode` so new moments can be bound to a specific Branch. Added a gap chooser popover that appears when clicking the root-area gap of a populated limb, offering "Add moment here" (existing behaviour) or "Start new branch" (creates a sibling Branch on the same Limb with a fanned `mapAngleOffset` via `computeNextBranchOffset`, then opens guided flow tagged to the new branch via `pendingNewBranchId`). Surfaced all branches in DevPanel under a new "BRANCHES (RAW)" section so new branches can be inspected immediately. Note: Slice 2 will refactor the render loop to visually fan sibling branches at their angle offsets; until then, new-branch moments persist correctly but render on the default limb trunk.
- Added optional `Moment.subtype` tagging (Option C for in-limb categorisation). The `People & Relationships` limb now ships a curated vocabulary (`family`, `friends`, `romance`, `community`) exposed via `LIMB_SUBTYPES` in `src/lib/types.ts`; other limbs keep empty vocabularies for now. Extended the `Moment` Prisma model with a nullable `subtype` column (`npx prisma db push` applied), wired `/api/moments` POST and `/api/moments/[id]` PATCH to accept and persist `subtype`, propagated `subtype` through the mock/load pipeline in `pathfinder-home.tsx`, and added a compact “Category” chip picker in the moment detail panel that only appears for limbs with defined subtypes. Pre-tagged the 7 People mock moments with representative subtypes so the feature is visible immediately. No visual changes to the map itself.
- Introduced canonical life-map foundations: new `Self/Limb/Branch/Moment/TurningPoint/Path/Gap` type system, new `LIMBS` config, new `mock-data` branch+moment fixtures, and rewritten map geometry helpers (`buildPaths`, `buildGaps`, `calculateMapPosition`) based on `mapPosition`.
- Added new REST endpoints for canonical entities (`/api/moments`, `/api/moments/[id]`, `/api/branches`) and set `/api/goals` + `/api/goals/[goalId]` to `308` redirects for backward compatibility with existing clients.
- Reworked Prisma schema life-map persistence from legacy node/line models to `Branch` + `Moment` models (plus auth models), updated dev reset/node helpers to use `Moment`, and aligned mock toggle usage to `FLAGS.MOCK_DATA`.
- Removed year/month prompts from guided moment flow (now type -> describe -> done), defaulted guided-flow saves to `year = currentYear` and `month = null`, and added a subtle timeline hint (`Year not set — tap to edit`) for current-year moments with no month so users can correct dates later in the detail panel while map ordering remains `mapPosition`-driven.
- Implemented Option A depth-ring hierarchy in the life map (`RING_1`, `RING_2`, `RING_3`) and updated fork/prompt/node positioning to create clearer near-root branching and farther-out node placement.
- Updated sub-branch spreads for all five default branches (wider angle offsets) to fan sub-branches earlier and increase visual separation.
- Added branch/sub-branch focus state (`focusedBranch`, `focusedSubbranch`) with dimming/scaling of non-focused branches, clickable branch/sub-branch lines/labels, breadcrumb-style back navigation on-map, and a focused sub-branch edit toggle with lightweight node edit handles.
- Added Ring-2 `+ Add` affordances: always visible for empty sub-branches, and hover/focus-visible for populated sub-branches at an add point beyond the last node.
- Refined edge hierarchy rendering weights/opacities by semantic depth (`trunk`, `subbranch`, `node`) and removed development-only branch position/node count logging effects that were adding noise and avoidable render churn.
- Enabled `DRAG_TO_CONNECT` and implemented first-pass node drag interactions on the life map: drag start/move/end with mouse and touch, ghost edge + ghost node visuals past a distance threshold, branch-near highlighting and “Connect to …?” affordance, drop-to-open guided flow continuation on the same branch/subbranch, and drop-near-other-branch calls to `POST /api/life-map/drag-suggestion` with a lightweight suggestion toast.
- Added drag guided-flow context (`parentNodeId`, `parentLabel`, `parentYear`, `subbranch`, `promptOverride`) and applied it to guided prompt copy plus year-chip generation so year choices start from the parent node’s year during drag-initiated flows.
- Life-map branch visuals now render with explicit trunk/fork structure per default branch: shared trunk from root to fork (`TRUNK_RATIO = 0.35`), visible fork dot, and per-sub-branch lines from fork to first node (or empty endpoint) using sub-branch colours/opacities; node placement uses fork-based formulas (`getForkPoint`, `getSubbranchPromptPos`, `getSubbranchNodePos`) so sub-branches fan out instead of stacking on one line.
- Updated default branch sub-branch metadata to complete configs (ids, labels, colors, angle offsets, examples) for Finance, Work, Becoming, People, and Health; renamed people romance id to `people-romance` and finance decisions id to `finance-decisions`.
- Added clickable empty sub-branch endpoint prompts (dashed mini circle + tiny label + hover CTA) that open guided flow preselected to that sub-branch and skip type selection by jumping directly into the mapped flow.
- Updated mock data subbranch ids to match branch config (`people-romance`, `finance-decisions`) so seeded nodes render on intended sub-branches.
- **Life map pillars:** Removed the default **Where I've Been** branch and all places-specific guided flows, mock nodes, and map styling. Added **Who I'm Becoming** as a fifth default pillar (not unlockable) with sub-branches Mindset Shifts, Lessons Learned, and Identity Changes; rebalanced default branch angles to Finance −72°, Work −36°, Becoming −2° (centre), People 36°, Health 72°.
- **Location on nodes:** Optional `location` string on life-map nodes; guided flow now includes an optional “Where were you when this happened?” step after the main flow (before significance) on all branches; map labels show `📍` plus location under the title at 7px and 35% opacity in branch colour. Prisma `LifeMapNode.location` added; `GET /api/goals` and `PATCH` life-map node payloads support `location`.
- **Becoming guided flows:** Concrete step flows for Mindset Shift, Lesson Learned, and Identity Change (including free-text steps for personal answers).
- Mock data: removed all places-branch nodes; added **Moved to Bangkok** on People (`mock-move1`), three **Who I'm Becoming** nodes, and sample `location` values on listed work/health/people nodes; updated cross-node `connectedTo` references.
- Added feature flags `DRAG_TO_CONNECT`, `AI_DRAG_SUGGESTIONS`, and `NODE_CONNECTIONS` (all default false) for a future drag-to-connect and AI suggestion flow on the life map.
- Added position-aware line insertion on the life map: trunk clicks now segment to sub-branch selection by click band, sub-branch clicks now compute insertion index from click ratio (start/middle/end), and guided flow receives insertion context (`insertIndex`, adjacent node ids, contextual `promptOverride`).
- Added hover insertion affordance on branch lines: a snapped “Add here” marker (green dot + branch-colored halo/label) follows cursor on transparent hit lines and clears on leave/close.
- Updated quick-node creation to accept a `createdAt` override and derive insertion-aware timestamps so non-end inserts remain correctly ordered by `createdAt` and shift existing nodes outward without changing map geometry rules.
- Added development-only `DevPanel` overlay (`FLAGS.DEV_PANEL`) with live hovered-element inspector, selected-node payload viewer, map/perf stats, branch breakdown explorer, quick dev actions, and a gap-zone visualiser toggle.
- Added `DEV_PANEL` feature flag and map wiring for dev hover payloads (`node`, `gap`, `branch`, `subbranch`), temporary map highlight from panel item clicks, keyboard toggle (`Cmd/Ctrl+Shift+D`), and a bottom-left `DEV` pill toggle.
- Updated user-facing language across dashboard/life-map/dev panel flows to simplify wording from goal/node terminology to moment-oriented copy (buttons, labels, empty states, and panel headings) without changing variable/type/API identifiers.
- Simplified life-map MVP to five fixed branches with no subbranches/unlocks, linear createdAt-based moment stacking, root-aware centering with dev-panel width compensation, single-line branch edge behavior, and mock data aligned to branch-only moments.
- Added line-system scaffolding across geometry/types/schema/mock data (new `map-geometry` utilities, `Line` and `LifeMapMoment` type extensions, Prisma `Line` model + moment line fields, and mock line fixtures with `lineId` on moments), plus memoized component shell files for upcoming life-map split.
- Added `mapPosition` ordering support for life-map moments (schema + types + mock data), switched map ordering to `mapPosition` with midpoint insertion support, updated timeline ordering to year/month ascending, and added optional guided-flow month-part capture mapped to month buckets.
- Fixed map/timeline ordering separation by introducing explicit guided-flow insert context for gap clicks and mapPosition midpoint insertion math, enforcing map rendering order by `mapPosition` only (independent from year), while keeping timeline rendering year/month ordered.
- Added `src/lib/types.ts` with scaffold types `DragState`, `NodeConnection`, and `AIDragSuggestion` for that feature.
- Scaffolded drag-related state, placeholder drag handlers, and a gated SVG connection-line placeholder in `pathfinder-home.tsx`; renamed the existing guided-flow connection type to `GuidedFlowConnection` to avoid clashing with the new shared `NodeConnection` type.
- Added scaffold API route `POST /api/life-map/drag-suggestion` returning a mock suggestion and `scaffolded: true` when authenticated.
- Reworked unlock-branch floating prompt: dismiss actions persist (`pathfinder_dismissed_unlocks` for “Not interested”, session snooze for “Remind me later”), queue + 30s gate between prompts, 10s auto-dismiss with leave animation, richer layout (branch icon/name/sublabel, contextual insight copy for “Who I'm Becoming”, social hint, countdown bar), repositioned above Today’s Focus, and three-button actions (add / remind / not interested).
- Life map nodes: hover shows a compact preview card; click opens a right-hand detail panel (slide-in, dimmed map) with description, debounced reflection (`timelineNote`), connections, significance controls, mini timeline, edit/save, branch move, and remove-with-confirm; map nodes dim other points when one is selected and add hover scale plus a selected ring.
- `GET /api/goals` now returns persisted `lifeMapNodes` from Prisma; `PATCH /api/goals/[goalId]` supports field updates for life map nodes (and basic goal field updates when the id is a goal) while preserving the existing conversation-based goal edit flow.
- Fixed runaway re-renders: `PerfMonitor` no longer calls `setState` on every commit (ref-count + throttled display); suggestions and dev logging effects no longer depend on a freshly allocated `timelineGoals` array each render; unlock-queue orchestration depends on `goals.length` instead of the full `goals` reference.
- Life map geometry: shared trunk to a fork point (`PROMPT_DISTANCE * 0.4`), sub-branch nodes placed from fork along per-sub angles (`0.6 * PROMPT_DISTANCE + index * NODE_SPACING`); edges draw one trunk curve, fork→first node, then node-to-node chains; decorative baselines skip duplicate trunk when sub-branches exist. `addOriginNodes` / `deduplicateNodes` drop synthetic `origin-born-living-place` when a `places-roots` node already exists (removes duplicate London story with mock “Grew up in London”).

## 2026-04-22

- Fixed life map branch rendering geometry to keep visible branches growing upward from the root.
- Updated branch node placement to render multiple nodes per branch using ordered branch-node loops.
- Updated fork positioning to derive offsets from parent branch distance and side.
- Updated edge path generation to use dedicated root-edge and branch-edge curve formulas.
- Added development diagnostics for branch position vectors and per-branch node counts.
- Enforced hidden state for unlockable branches in mock-mode initialization by resetting unlocked branches to empty on load.
- Added sub-branch configuration for default life-map branches with per-subbranch angle offsets, color tints, and labels.
- Updated node positioning and edge generation to group branch nodes by `subbranch` and render branching paths from a short trunk.
- Added guided overlay first-step sub-branch selection cards per default branch before example-pill flows.
- Added `subtype` and `subbranch` fields to `LifeMapNode` in Prisma schema and propagated fields through map node transforms.
- Updated mock life data to include explicit `subbranch` and `subtype` metadata for work, health, people, places, and finance nodes.
- Scoped wheel handling by active view using dedicated map/timeline containers to prevent map zoom and timeline scroll conflicts.
- Added dedicated map and timeline wrapper containers (`display` gated by `activeView`) so hidden views cannot intercept wheel/mouse events.
- Added map-only zoom controls and preserved pan/zoom state across view switches with initial auto-fit guard.
- Added timeline scroll reset on timeline activation for consistent re-entry position.
- Added life-map undo history (last 5 actions) with floating undo pill, keyboard shortcut (`Cmd/Ctrl+Z`), and toast feedback.
- Added right-click node context menu with "Remove from map" action and outside-click dismissal.
- Added node deletion handling through `DELETE /api/goals/:id` for `LifeMapNode` ids while preserving existing goal delete behavior.
- Removed implicit auto-unlock-by-existing-goals behavior and enforced branch visibility from explicit unlock state only.
- Updated `Where I've Been` branch angle to reduce sideways drift and fan it upward-right more naturally.
- Reworked places sub-branches into roots/lived/visited/wanted with new offsets and metadata.
- Added load-time node deduplication by both id and label+branch to prevent duplicate place nodes.
- Updated places guided flow variants to roots/lived/visited/wanted journey types.
- Added special map styling for `places-wanted` nodes/lines (dashed branch line, hollow nodes, italic star labels).
- Updated mock places data to include visited and wanted nodes and removed redundant duplicate roots entry.
